<result>{
for $a in document("j_caesar.xml")//ACT,
    $sc in $a//SCENE,
    $sp in $sc/SPEECH,
	$l in $sp/LINE
where $l/text() = "Et tu, Brute! Then fall, Caesar."
return <who>{$sp/SPEAKER/text()}</who>,
       <when>{<act>{$a/TITLE/text()}</act>,
             <scene>{$sc/TITLE/text()}</scene>,
					<line>{$l/text()}</line>}
       </when>
}</result>